#ifndef _LUCID_UTILS_
#define _LUCID_UTILS_

//#include <gli/gli.hpp>

#include <string>
#include <fstream>

class LUtils {

public:
	static std::string    readFile (const std::string&);
	//static gli::texture2D loadDDS (const std::string&);
	
};

#endif